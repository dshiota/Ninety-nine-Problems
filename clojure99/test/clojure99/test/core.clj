(ns clojure99.test.core
  (:use [clojure99.core])
  (:use [clojure99.list])
  (:use [clojure99.arithmetic])
  (:use [clojure.test])
  (:use [clojure.pprint]))

; (load "test_list")
(load "test_arithmetic")
